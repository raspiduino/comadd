wsession = tk.Toplevel(gui)
    #wsession.title("Edit session setting")
    ttk.Label(wsession, text="Session time (in sec): ").pack()
    stimeinput = ttk.Entry(wsession, width=10)
    stimeinput.pack()
    ttk.Label(wsession, text="Break time (in sec): ").pack()
    btimeinput = ttk.Entry(wsession, width=10)
    btimeinput.pack()

    # Create check button
    def edit(num, val):
        settings[num] = val
        wsetting()

    ttk.Label(wsession, text="Advanced settings?").pack()
    lb1 = ttk.Label(wsession, text=("Currently: " + ("Enable" if settings[0] == "1" else "Disable")))
    lb1.pack()
    def config1():
        edit(0, ("1" if settings[0] == "0" else "0"))
        lb1.configure(text=("Currently: " + ("Enable" if settings[0] == "1" else "Disable")))
        bt2.configure(text=("Enable" if settings[0] == "0" else "Disable"))
    bt1 = ttk.Button(wsession, text=("Enable" if settings[0] == "0" else "Disable"), command=config1)
    bt1.pack()
    ttk.Label(wsession, text="Block Internet?")
    lb2 = ttk.Label(wsession, text=("Currently: " + ("Enable" if settings[1] == "1" else "Disable")))
    lb2.pack()
    def config2():
        edit(1, ("1" if settings[1] == "0" else "0"))
        lb2.configure(text=("Currently: " + ("Enable" if settings[1] == "1" else "Disable")))
        bt2.configure(text=("Enable" if settings[1] == "0" else "Disable"))
        print(settings[1])
    bt2 = ttk.Button(wsession, text=("Enable" if settings[1] == "0" else "Disable"), command=config2)
    bt2.pack()